rules_version = '2';

// All reads/writes go through the Next.js server using the Firebase Admin SDK,
// which bypasses these rules. Client-side access is intentionally denied.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
