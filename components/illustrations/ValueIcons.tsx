import { BookOpen, Rocket, PartyPopper, Sparkles, type LucideIcon } from "lucide-react";

export const valueIconMap: Record<string, LucideIcon> = {
  autonomy: Rocket,
  creativity: Sparkles,
  sustain: BookOpen,
  fun: PartyPopper,
};
